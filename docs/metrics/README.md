# Metrics Archive

Store dated baseline and follow-up measurements here using the filename format `YYYY-MM-DD-description.md`.
Each entry should note the commands executed, raw outputs (or artifact paths), and follow-up actions if any checks fail.
